spring:
  liquibase:
    enabled: true
    user: postgres
    password: postgres
  datasource:
    url: 'jdbc:postgresql://localhost:5432/'
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  cloud:
    openfeign:
      client:
        config:
          frame-state-client:
            url: http://localhost:8082
